import { SurnameFormControl } from './surname-form-control';

describe('SurnameFormControl', () => {
  it('should create an instance', () => {
    expect(new SurnameFormControl()).toBeTruthy();
  });
});
